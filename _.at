
// id , the well-known:     \a.a        ===  func id(foo) {return foo}
self := _

// flip:                    \fab.fba    === func flip(f) {return func(a,b) {return f(b,a)}}
f ~ a b :=
    x tmp2, x := a, tmp2 := tmp1
    tmp1 := bb1 ff2, bb1 := b, ff1 := f
    ff2 := f

// 1 alone comment
// 2 alone comment

// const, the well-known:   \ab.a       === func const(x) { return func(y) {return x}}
tho := fst
