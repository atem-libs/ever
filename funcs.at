
x it := x

ever := true

fun : arg := arg fun

arg . fun := arg fun

// read as 'of' --- "hello world" toUpper</trim
fn2 </ fn1 arg := (arg fn1) fn2

// read as 'andThen' --- "hello world" trim/>toUpper
fn1 /> fn2 arg := (arg fn1) fn2

x == y := _

want § have :=
    have ? want => have
